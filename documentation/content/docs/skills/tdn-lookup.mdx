---
title: TDN Lookup
description: Estrategia de busca online no TDN (TOTVS Developer Network) via API REST do Confluence com fallback em 4 tiers
---

# TDN Lookup — Estrategia de Busca Online

Estrategia centralizada para buscar documentacao no TDN (TOTVS Developer Network) usando a API REST do Confluence. Utilizada automaticamente por todos os agents que precisam consultar documentacao online da TOTVS.

## Quando e Usada

- Quando a referencia local (`protheus-reference`) nao cobre a funcao ou classe consultada
- Para validar pontos de entrada (PARAMIXB, retorno, rotina chamadora)
- Para confirmar assinaturas de metodos nao documentados localmente
- Para buscar documentacao de classes/funcoes de nicho

## Tiers de Busca

A busca segue 4 tiers de fallback, progredindo automaticamente quando um tier falha:

| Tier | Metodo | Descricao |
|------|--------|-----------|
| Tier 2 | WebFetch API | Busca via API REST do Confluence (CQL query) — mais rapido e eficiente |
| Tier 3 | Playwright API JSON | Mesmo endpoint via Playwright (quando WebFetch falha por CORS/timeout) |
| Tier 4a | WebSearch + Playwright | Busca via motor de busca + navegacao com Playwright |
| Tier 4b | Playwright Visual | Navegacao direta no TDN com snapshot visual |

## Consulta CQL

A busca usa Confluence Query Language (CQL) para filtrar resultados:

- **Busca exata:** `type=page AND title="{NOME}" AND space IN ("tec","framework")`
- **Busca fuzzy:** `type=page AND text~"{NOME}"`

## Dados Extraidos

Apos encontrar a pagina no TDN, os seguintes dados sao extraidos:

- Sintaxe e parametros da funcao/classe
- Tipo de retorno e valores possiveis
- Exemplos de uso
- Observacoes e restricoes de versao
- Para pontos de entrada: PARAMIXB (tipos, posicoes), retorno esperado, rotina chamadora
